.DS_Store
*.backup-*

# Machine-specific overrides live outside the repo on purpose.
.zshrc.local

# Agent tooling. AGENTS.md, CLAUDE.md and .claude/skills/ are shared and
# checked in. Per-machine settings are not.
.claude/settings.local.json
CLAUDE.local.md
